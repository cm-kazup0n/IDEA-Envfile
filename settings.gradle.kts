rootProject.name = "idea envfile plugin"
pluginManagement {
    repositories {
        maven(url = "https://oss.sonatype.org/content/repositories/releases/")
        maven(url = "https://www.jetbrains.com/intellij-repository/releases/")
        maven(url = "https://plugins.gradle.org/m2/")
        gradlePluginPortal()
    }
}

